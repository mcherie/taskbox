// console.log(d3)

// d3.select("#d3-div") // select/All element/s on the DOM
//     .selectAll("p") // selects everything inside this div
//     .data([1, 2, 3]) // BIND. this selection into data, which is usually an array
//     .enter() // for all the missing
//     .append("p") // append a [new] p to all the missing data
//     .text(dta => dta)